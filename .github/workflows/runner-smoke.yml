name: Runner Smoke Test

on:
  push:
    branches: [ accuracy-pipeline-v216 ]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-22.04
    steps:
      - name: Runner is alive
        run: |
          echo "runner-ok"
          uname -a
